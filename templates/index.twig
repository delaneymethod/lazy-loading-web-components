{% extends '_layouts/default.twig' %}

{% from '_macros/component.twig' import component %}

{% block content %}

	<code>/templates/_components/my-example</code>
	{{ component('my-example', {
		backgroundColor: 'grey',
		text: 'This is an example.'
	}) }}

	{#
	<code>/templates/_components/my-slider</code>
	{{ component('my-slider') }}

	<code>/templates/_components/my-lightbox</code>
	{{ component('my-lightbox') }}
	#}
{% endblock %}
